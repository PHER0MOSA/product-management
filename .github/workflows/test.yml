name: Test
on:
  pull_request:
    paths: ['server/**/*.go']
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: 1.24.6
      - run: cd server && go test ./...

